<h1>Listing mails <%= "Total : #{@messages.count}" %></h1>
<table>
  <thead>
    <tr>
      <th>Subject</th>
      <th>Sender</th>
      <th>id</th>
      <th>Date</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @messages.each do |message| %>
      <tr>
        <td><%= message.subject %></td>
        <td><%= message.sender %></td>
        <td><%= message.id %></td>
        <td><%= message.date %></td>
        <td><%= link_to 'Show', message %></td>
        <td><%= link_to 'Edit', edit_message_path(message) %></td>
        <td><%= link_to 'Destroy', message, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Mail', new_message_path %>
